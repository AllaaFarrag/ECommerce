import { FilteredCategoryPipe } from './filtered-category.pipe';

describe('FilteredCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new FilteredCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
